# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Standalone release exports (produced by `npm run export`).
export/

# GPUI experiment under native/: a vendored gpui-ce checkout (its own git clone,
# ~1 GB) and the Rust build output (~9 GB). Deliberately narrower than `native/`
# so first-party sources added there are still staged.
native/target/
native/vendor/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
