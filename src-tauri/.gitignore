# Generated by Cargo
# will have compiled files and executables.
# Unanchored so it also covers the path-dependency crates (exrthumb, psdcomp,
# ...), which each build into their OWN target/ directory. The anchored `/target/`
# alone let psdcomp/target through — several hundred .o files and an incremental
# cache, staged in one `git add`.
target/

# Generated by Tauri
# will have schema files for capabilities auto-completion
/gen/schemas
